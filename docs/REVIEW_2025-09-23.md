## Completed Foundations
- **Configuration, metrics, and walk-forward orchestration** — `xdte.config.Settings`, the metrics helpers, and the walk-forward splitter all ship with deterministic defaults, CLI/env override support, and unit coverage, matching the behaviours captured in the exploratory notebook workflow.【F:src/xdte/config.py†L1-L139】【F:src/xdte/metrics.py†L1-L98】【F:src/xdte/model/wfo.py†L1-L199】
- **Backtest loaders and schema contract** — `xdte.data.loaders` ingests the production CSV bundle (including BOM-trimmed headers, split date/time columns, leg parsing, session tagging, and tenor inference) and is documented in `docs/DATA_SCHEMA.md`, with fixtures + validation tests verifying the frozen dataset manifests.【F:src/xdte/data/loaders.py†L1-L308】【F:docs/DATA_SCHEMA.md†L1-L129】【F:tests/unit/test_validation.py†L1-L48】
- **Feature engineering layer** — `xdte.data.features` now provides deterministic trade panels, market pivots, lagged daily context, and t=0 VIX deltas consistent with the roadmap kill-switch inputs, again backed by fixture-driven tests.【F:src/xdte/data/features.py†L1-L602】【F:tests/unit/test_features.py†L1-L94】
- **CLI + data validation scaffolding** — The Click application exposes `train`, `tune`, `export-kit`, `decide`, and `validate-backtest` entry points with shared settings options, directory guards, and manifest checking for the uploaded bundle.【F:src/xdte/cli.py†L1-L375】【F:tests/test_cli.py†L1-L93】
- **Legacy selector retired** — The former `xdte_ml_selector.MLSelector` shim and its tests have been removed so contributors focus exclusively on the new pipeline modules under `xdte`.【F:README.md†L63-L67】

## Outstanding Gaps After Latest Sync
1. **Model lifecycle lacks automated validation.** `train_models`, `discover_rules`, and `apply_models` now generate LightGBM folds, winsorised thresholds, and scored metrics, but no unit or integration suite exercises those paths beyond CLI monkeypatches, so artifact layouts and numerical stability remain unguarded.【F:src/xdte/model/train.py†L210-L373】【F:src/xdte/model/discovery.py†L141-L247】【F:src/xdte/model/apply.py†L186-L279】【F:tests/test_cli.py†L25-L214】
2. **Hybrid/export hardening is still pending.** `select_hybrid_policy` and `build_live_kit` enforce kill-switch rules and emit manifests, yet they ship without acceptance tests or schema documentation, leaving tail/gamma constraints and digest integrity unchecked outside of CLI mocks.【F:src/xdte/model/hybrid.py†L1-L360】【F:src/xdte/model/export.py†L1-L151】【F:tests/test_cli.py†L216-L272】
3. **Live decisioning stops at policy replay.** `run_live_decisions` loads the Live Kit and converts the stored hybrid policy into `SET_TAILS`/`ADJUST_GAMMA` outputs using caller-supplied market snapshots, but it never scores the exported models or integrates with live data feeds, so inference remains manual.【F:src/xdte/live/decide.py†L221-L327】【F:src/xdte/cli.py†L492-L566】
4. **Golden regression suite is unfinished.** `tests/golden_manifest.json` still contains placeholder hashes and artifacts, and no parity test exercises the new data ingestion/feature stack.【F:tests/golden_manifest.json†L1-L79】

## Data-Aware Tasks to Finish the Roadmap
### T2 — Data & Features *(Status: Complete)*
- No further action required beyond minor refactors; loaders, fixtures, schema docs, and feature tables already reflect the uploaded bundle.【F:src/xdte/data/loaders.py†L1-L308】【F:src/xdte/data/features.py†L1-L602】【F:docs/DATA_SCHEMA.md†L1-L129】

### T3 — Model Lifecycle
- Add deterministic unit/integration coverage around `train_models`, `discover_rules`, and `apply_models`, using frozen backtest slices to verify artifact layouts, manifests, and numeric summaries under edge cases such as sparse folds.【F:src/xdte/model/train.py†L210-L373】【F:src/xdte/model/discovery.py†L141-L247】【F:src/xdte/model/apply.py†L186-L279】
- Capture the produced schema (file naming, columns, metadata fields) in developer docs so future contributors understand the persisted contract before layering on tuning/export automation.【F:src/xdte/model/train.py†L294-L370】【F:src/xdte/model/discovery.py†L200-L244】【F:src/xdte/model/apply.py†L246-L276】

### T4 — Hybrid Policy & Export
- Build scenario coverage for `select_hybrid_policy`, exercising safety guardrails, baseline comparisons, and selection scoring so we can trust accepted policies in CI.【F:src/xdte/model/hybrid.py†L237-L360】
- Write contract tests (and supporting docs) for `build_live_kit`, ensuring manifests/digests reflect the expected directory layout and surfacing checksum drift clearly.【F:src/xdte/model/export.py†L78-L151】

### T5 — Golden Parity & Regression
- Produce a real golden bundle (artifacts + hashes) from `data/backtest_data/original/`, update `tests/golden_manifest.json`, and author `tests/test_golden_parity.py` to compare fresh runs against the frozen outputs within documented tolerances.【F:data/backtest_data/original/11am_market_stats.csv†L1-L11】【F:tests/golden_manifest.json†L1-L79】

### T6 — CLI & Live Decisioning
- Add end-to-end tests (or smoke scripts) that exercise `xdte decide` with sample market/daily payloads so feature assembly, warnings, and emitted actions stay stable.【F:src/xdte/cli.py†L492-L566】【F:src/xdte/live/decide.py†L137-L327】
- Extend `run_live_decisions` and the CLI wiring to score exported models or otherwise integrate with live data providers, rather than replaying stored hybrid policy knobs only.【F:src/xdte/live/decide.py†L248-L327】【F:src/xdte/cli.py†L492-L566】

### Cleanup & Documentation
- Expand design/operational docs to reference the concrete data and artifact contracts established above, pointing contributors to the `xdte` package as the single source of truth.【F:README.md†L63-L67】【F:docs/DATA_SCHEMA.md†L1-L129】

## Immediate Next Steps
1. Backfill tests and documentation for the training → discovery → apply stack so fold outputs, manifests, and metrics stay deterministic across refactors.【F:src/xdte/model/train.py†L210-L373】【F:src/xdte/model/discovery.py†L141-L247】【F:src/xdte/model/apply.py†L186-L279】
2. Define the persisted artifact contract (directory layout, manifests, hashes) and seed a real golden bundle for regression testing.【F:tests/golden_manifest.json†L1-L79】
3. Extend the live decision workflow to run exported models (or otherwise integrate with real-time data providers) instead of replaying static policy knobs, enabling end-to-end inference from the Live Kit.【F:src/xdte/live/decide.py†L221-L327】【F:src/xdte/cli.py†L492-L566】
